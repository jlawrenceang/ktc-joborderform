# Delivery scorecard — one row per ship

The 4-metric outcome record (the global playbook's measurement habit): filled at each `/ship`,
appended by `/wrap`, read by the owner monthly. Tolerance: **red-zone escapes stay at 0**.
A metric earns its column only if it can trigger a specific action — don't add vanity columns.

| Date | Change (one line) | Escaped defect? | Rework needed? | Merged unverified? | Lead time (ask→live, coarse) | Notes |
|---|---|---|---|---|---|---|
